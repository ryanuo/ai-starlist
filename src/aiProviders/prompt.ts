export function buildPrompt(text: string): string {
  return `
根据以下项目列表，自动分析每个项目的功能、技术栈和应用场景，并为每个项目生成一个或多个最合适的分类。

请按 CSV 格式输出，每行包含“分类名称, 项目名称”：
示例格式：
分类名称1, 项目名称1
分类名称1, 项目名称2
分类名称2, 项目名称3

以下是项目列表：${text}
`
}
